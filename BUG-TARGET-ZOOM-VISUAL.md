# БАГ: Zoom режим не увеличивает мишень визуально

**Дата:** 2024-08-19  
**Приоритет:** HIGH  
**Статус:** ✅ ИСПРАВЛЕНО  
**Связанный план:** plans/PLAN-TARGET-ZOOM.md

---

## Описание проблемы

При переключении на zoom режим (🔍 7-10) **визуально не происходит увеличения мишени**.

**Текущее поведение:**
- Клик на кнопку 🔍 → меняется цвет центральной зоны (исчезает чёрный круг)
- Внешние кольца 1-6 исчезают
- НО размер центральной зоны 7-10 остаётся **таким же маленьким**
- Выстрелы не увеличиваются

**Ожидаемое поведение (как в фотоаппарате):**
- Клик на кнопку 🔍 → центральная зона 7-10 **увеличивается** и занимает всё доступное пространство viewBox
- Кольца 7, 8, 9, 10 становятся **визуально больше**
- Выстрелы **увеличиваются** пропорционально и остаются на своих местах относительно колец
- Эффект "приближения камеры" — как будто зум-объектив приблизил центр мишени

---

## Воспроизведение

1. Открыть тренировку
2. Сделать несколько выстрелов в зону 7-10
3. Кликнуть на кнопку 🔍 (zoom)
4. **Баг:** выстрелы остаются маленькими точками, центральная зона не увеличивается

---

## Анализ причины

### Текущая реализация (src/components/TargetCanvas.tsx):

```typescript
const VIEW = 160; // viewBox всегда 160x160
const SVG_TARGET_RECT = { left: 0, top: 0, width: VIEW, height: VIEW }; // константа

// В zoom7 режиме:
const outerRadius = zoomMode === 'zoom7' ? RING_D[7] / 2 : 80; // 29.75 vs 80

// SVG:
<svg viewBox={`0 0 ${VIEW} ${VIEW}`}>
  <circle cx={CENTER} cy={CENTER} r={outerRadius} fill="white" />
  // кольца 7-10 с теми же радиусами RING_D[7-10] / 2
</svg>
```

**Проблема:**
1. `viewBox` остаётся `0 0 160 160` — видимая область не меняется
2. `outerRadius = 29.75` — белый круг уменьшается до размера зоны 7
3. Координаты выстрелов НЕ масштабируются
4. `targetToScreen()` использует константный `SVG_TARGET_RECT` — не учитывает zoom

**Результат:** вместо увеличения центра мы просто **рисуем маленький круг** в том же viewBox.

---

## Решение

Нужно **масштабировать координаты** так, чтобы зона радиусом 29.75mm занимала те же 80 единиц viewBox, что и полная мишень.

### Вариант 1: Масштабирование через коэффициент (рекомендую)

**Коэффициент zoom:**
```typescript
const ZOOM_SCALE = zoomMode === 'zoom7' ? 80 / (RING_D[7] / 2) : 1.0;
// zoom7: 80 / 29.75 ≈ 2.69
// full:  1.0
```

**Изменения:**

1. **Координаты колец** масштабировать:
   ```typescript
   <circle r={RING_D[7] / 2 * ZOOM_SCALE} /> // 29.75 * 2.69 = 80
   <circle r={RING_D[8] / 2 * ZOOM_SCALE} /> // и т.д.
   ```

2. **Координаты выстрелов** масштабировать перед `targetToScreen`:
   ```typescript
   const scaledXh = shot.x * ZOOM_SCALE;
   const scaledYh = shot.y * ZOOM_SCALE;
   const sp = targetToScreen(scaledXh, scaledYh, SVG_TARGET_RECT);
   ```

3. **Координаты ввода** (tap/drag) демасштабировать после `screenToTarget`:
   ```typescript
   const target = screenToTarget(clientX, clientY, rect);
   const actualXh = target.xh / ZOOM_SCALE;
   const actualYh = target.yh / ZOOM_SCALE;
   ```

4. **Радиусы лабелей** масштабировать:
   ```typescript
   const ringLabels = zoomMode === 'zoom7'
     ? RING_LABELS_ZOOM7.map(l => ({ ...l, r: l.r * ZOOM_SCALE }))
     : RING_LABELS_FULL;
   ```

---

### Вариант 2: Динамический viewBox (сложнее)

```typescript
const viewBoxSize = zoomMode === 'zoom7' ? RING_D[7] : VIEW;
const viewBoxOffset = zoomMode === 'zoom7' ? CENTER - RING_D[7]/2 : 0;

<svg viewBox={`${viewBoxOffset} ${viewBoxOffset} ${viewBoxSize} ${viewBoxSize}`}>
```

**Минусы:**
- Сложнее, нужно пересчитывать центр
- SVG_TARGET_RECT тоже должен меняться
- Больше мест для ошибок

---

## Критерии приёмки исправления

### Визуальный zoom
- ✅ В режиме zoom7 центральная зона 7-10 **визуально увеличивается** и занимает всё доступное пространство
- ✅ Кольца 7, 8, 9, 10 становятся **крупными**, как при приближении камеры
- ✅ Выстрелы **увеличиваются** пропорционально
- ✅ Выстрел в центр (10.9) остаётся визуально в центре
- ✅ Выстрел на границе 7-го кольца остаётся на границе (но визуально ближе к краю)
- ✅ Tap/drag работает корректно — попадание в центр даёт 10.9
- ✅ Переключение full ↔ zoom7 корректно масштабирует выстрелы туда-обратно

### UI: Циклическая кнопка zoom
- ✅ **Одна кнопка** вместо двух состояний (убрать 🎯/🔍)
- ✅ Кнопка показывает **текущий режим**: "1-10" или "7-10"
- ✅ Клик по кнопке → **циклическое переключение**: "1-10" → "7-10" → "1-10"
- ✅ Дизайн готов к добавлению промежуточных режимов (например "8-10")
- ✅ Настройка `targetZoomMode` остаётся совместимой ('full' | 'zoom7')

### Тесты
- ✅ Все 254 теста проходят
- ✅ `transform.test.ts` не ломается (либо адаптируется)

---

## Изменяемые файлы

- `src/components/TargetCanvas.tsx` — основная логика масштабирования (ZOOM_SCALE коэффициент)
- `src/screens/TrainingScreen.tsx` — изменить кнопку с двух состояний на циклическую
- Возможно `src/transform.ts` — если нужно добавить параметр scale в targetToScreen/screenToTarget (но лучше обойтись без изменений)
- Возможно тесты `src/components/TargetCanvas.test.tsx` (если создадим)

**НЕ меняются:**
- `src/scoring.ts` — scoring остаётся в hundredths of mm
- `src/db/*` — хранение данных не меняется
- `src/domain/*` — бизнес-логика не затронута

---

## UI: Циклическая кнопка zoom

**До (текущее):**
```
Header: [◀ Назад]  [Иван Петров]  [🎯]  [№15]
                                    ↑
                           Показывает режим
                           Клик → переключает иконку
```

**После (новое):**
```
Header: [◀ Назад]  [Иван Петров]  [1-10]  [№15]
                                    ↑
                           Показывает текущий режим
                           Клик → циклическое переключение

1-10 → клик → 7-10 → клик → 1-10 → ...
```

**Стиль кнопки:**
```typescript
// Текст меняется в зависимости от режима:
zoomMode === 'full'   → "1-10"  // вся мишень
zoomMode === 'zoom7'  → "7-10"  // только центр

// В будущем добавим:
zoomMode === 'zoom8'  → "8-10"  // ещё больше zoom
```

**Преимущества:**
- Одна кнопка вместо двух состояний
- Явно показывает что видно на мишени
- Легко расширить: "1-10" → "7-10" → "8-10" → "9-10" → "1-10"
- Экономит место в header

---

## Визуальный пример ожидаемого поведения

**Full режим (🎯):**
```
┌─────────────────────────┐
│ 1 2 3 4 5 6 ███████████ │  <- полная мишень 1-10
│           ███ 7 8 9 10█ │     центр маленький
│           ███████████   │
└─────────────────────────┘
```

**Zoom7 режим (🔍):**
```
┌─────────────────────────┐
│                         │
│    ████████████████     │  <- только зона 7-10
│    ██ 7  8  9  10 █     │     УВЕЛИЧЕНА на весь экран
│    ████████████████     │
│                         │
└─────────────────────────┘
```

Выстрел в центр:
- Full режим: маленькая точка в центре чёрного круга
- Zoom7: **большая точка** в центре белого круга (10)

---

## Приоритет

**HIGH** — функционал уже в production, но работает не так, как ожидал пользователь.

---

## Назначение

**Worker** с чистым контекстом, после анализа этого баг-репорта.

---

## Исправление (2024-08-19)

**Worker run:** a0b2aace (57a9604a-e9fd-4df7-840e-fe3ea1363ee7)

**Реализовано:**

### 1. Визуальный zoom (TargetCanvas.tsx)
- ✅ Добавлен коэффициент `ZOOM_SCALE = 80 / (RING_D[7] / 2) ≈ 2.69` для zoom7
- ✅ Внешний белый круг всегда `r=80` (заполняет viewBox)
- ✅ Чёрная зона масштабируется: `RING_D[7] / 2 * ZOOM_SCALE = 80` в zoom7
- ✅ Радиусы колец 7-10 масштабируются × ZOOM_SCALE
- ✅ Координаты выстрелов масштабируются × ZOOM_SCALE перед `targetToScreen()`
- ✅ Входные координаты демасштабируются ÷ ZOOM_SCALE после `screenToTarget()` → scoring корректен
- ✅ Радиусы лабелей масштабируются × ZOOM_SCALE
- ✅ Центральная белая точка масштабируется: `2.5 * ZOOM_SCALE ≈ 6.7` в zoom7

### 2. UI: Циклическая кнопка (TrainingScreen.tsx)
- ✅ Константы `ZOOM_MODES = ['full', 'zoom7']` и `ZOOM_LABELS = { full: '1-10', zoom7: '7-10' }`
- ✅ Кнопка показывает текущий режим: "1-10" или "7-10"
- ✅ Клик → циклическое переключение: `(currentIdx + 1) % ZOOM_MODES.length`
- ✅ Код готов к расширению: добавить `'zoom8'` в массив → автоматически работает

### 3. Тесты
- ✅ Все 254 теста проходят (scoring.test, transform.test, db.test, domain.test)
- ✅ TypeScript компиляция чистая
- ✅ Vite build успешен (188.41 kB)

**Изменённые файлы:**
- `src/components/TargetCanvas.tsx` — масштабирование через ZOOM_SCALE
- `src/screens/TrainingScreen.tsx` — циклическая кнопка zoom

**Результат:**
- В режиме "7-10" центральная зона визуально увеличена и занимает весь экран
- Кольца 7-10 крупные, хорошо видны
- Выстрелы увеличены пропорционально
- Tap/drag работает корректно — координаты демасштабируются, scoring даёт правильные баллы
- Переключение "1-10" ↔ "7-10" корректно масштабирует всё туда-обратно

**Deployment:** Готов к production

**Deployment:**
- Commit: `87d9c1d`
- Production URL: https://musketoon-coach.pages.dev
- Preview URL: https://bb349edb.musketoon-coach.pages.dev
- Status: ✅ Live

**Проверка:**
1. Открой https://musketoon-coach.pages.dev
2. Создай/открой тренировку
3. Кликни кнопку "1-10" → переключится на "7-10"
4. **Мишень увеличится** — центральная зона займёт весь экран
5. Сделай выстрел в центр → должен дать 10.9
6. Переключи обратно на "1-10" → мишень вернётся к полному виду
