# PLAN: Ring Labels −1 (visual only)

## Задача
Визуально сместить цифры на мишени на −1: где отображается 10 → показать 9, 9 → 8, …, 1 → 0.
**Логика scoring/transform/DB не меняется.** Только отрисовка `<text>` в `TargetCanvas.tsx`.

## Файл
`src/components/TargetCanvas.tsx`

## Текущее состояние
Метки колец определены в двух массивах:

```ts
// RING_LABELS (full view): n = 9, 8, 7, 6, 5, 4, 3, 2, 1
const RING_LABELS = [
  { n: 9, r: ..., color: 'white' },
  { n: 8, ... },
  ...
  { n: 1, ... },
];

// RING_LABELS_ZOOM7: n = 10, 9, 8, 7
const RING_LABELS_ZOOM7 = [
  { n: 10, ... },
  { n: 9, ... },
  { n: 8, ... },
  { n: 7, ... },
];
```

В JSX метка рендерится как:
```tsx
>{n}</text>
```

## Изменение
В JSX при рендере метки заменить `{n}` на `{n - 1}`:

```tsx
>{n - 1}</text>
```

Это единственное изменение. Массивы `RING_LABELS` и `RING_LABELS_ZOOM7` трогать не нужно —
поля `n` используются для позиционирования (радиус `r` = `RING_D[n] / 2 + 2.5`), менять их нельзя.

## Acceptance criteria
- [ ] Full view: цифры 8, 7, 6, 5, 4, 3, 2, 1, 0 (вместо 9–1)
- [ ] Zoom7 view: цифры 9, 8, 7, 6 (вместо 10–7)
- [ ] Геометрия колец, цвета, логика скоринга — не изменены
- [ ] `npm test` — green (254 tests)
- [ ] `npm run build` — успешно

## Rollback
Один символ: вернуть `{n}` вместо `{n - 1}`.
