# БАГ: Zoom режим не показывает полную структуру мишени

**Дата:** 2024-08-19  
**Приоритет:** HIGH  
**Статус:** ✅ ИСПРАВЛЕНО  
**Связанный баг:** docs/bugs/BUG-TARGET-ZOOM-VISUAL.md (частично исправлен)

---

## Описание проблемы

После исправления визуального zoom мишень увеличивается, но **не выглядит как zoom существующей мишени**.

**Текущее поведение (zoom7):**
- Белый круг увеличен ✅
- Кольца 7-10 увеличены ✅
- **НО: чёрная зона НЕ рисуется** 🔴
- Выглядит как пустой белый круг с кольцами, а не как увеличенный центр мишени

**Ожидаемое поведение:**
- Zoom должен выглядеть как **фотография увеличенного центра** мишени
- Все элементы присутствуют: чёрная зона (кольца 7-8-9), белая десятка, белая точка в центре
- Просто всё **крупнее** — как будто камера приблизилась

---

## Воспроизведение

1. Открыть тренировку
2. Кликнуть кнопку zoom ("7-10")
3. **Баг:** Видно белый круг с кольцами 7-10, но **нет чёрной зоны** внутри
4. **Ожидание:** Видно чёрный круг (зона 7) с белыми кольцами 8-9-10 внутри

---

## Анализ причины

### Текущий код (src/components/TargetCanvas.tsx):

```typescript
{/* 2. Black zone: solid black circle up to ring-7 boundary (scaled for zoom) */}
<circle cx={CENTER} cy={CENTER} r={RING_D[7] / 2 * ZOOM_SCALE} 
  fill={isZoom7 ? 'none' : 'black'} />
                    ↑
              ПРОБЛЕМА! fill='none' в zoom7
```

**Баг:** В zoom7 режиме чёрная зона получает `fill='none'` — не рисуется вообще.

**Правильно:** Чёрная зона должна рисоваться ВСЕГДА, просто в zoom7 она масштабируется:
```typescript
// Чёрная зона ДО кольца 7 — всегда solid black:
<circle r={RING_D[7] / 2 * ZOOM_SCALE} fill="black" />
```

В zoom7 это даст радиус `29.75 * 2.69 = 80` — заполнит весь viewBox чёрным кругом, внутри которого будут белые кольца 8-9-10. **Именно так и должно быть!**

---

## Решение

### 1. Убрать условие `fill={isZoom7 ? 'none' : 'black'}`

```typescript
// БЫЛО (НЕПРАВИЛЬНО):
<circle r={RING_D[7] / 2 * ZOOM_SCALE} fill={isZoom7 ? 'none' : 'black'} />

// ДОЛЖНО БЫТЬ:
<circle r={RING_D[7] / 2 * ZOOM_SCALE} fill="black" />
```

**Почему это правильно:**
- **Full режим:** `ZOOM_SCALE = 1.0`, радиус = 29.75, чёрный круг внутри белого (как сейчас)
- **Zoom7 режим:** `ZOOM_SCALE = 2.69`, радиус = 80, чёрный круг заполняет viewBox
- Внутри чёрного круга рисуются белые кольца 8-9-10 — выглядит как увеличенный центр

---

### 2. UI кнопки: заменить текст на "Масштаб"

**Сейчас:**
```tsx
<button>{zoomMode === 'full' ? '1-10' : '7-10'}</button>
```

**Нужно:**
```tsx
<button>Масштаб</button>
// или с иконкой:
<button>🔍 Масштаб</button>
```

**Почему:**
- Одна понятная кнопка вместо меняющегося текста
- "Масштаб" интуитивно понятнее чем "1-10"/"7-10"
- Экономит место в header

**Альтернативы (на выбор):**
- Просто "Масштаб"
- "🔍 Масштаб" (с иконкой)
- "🔍" (только иконка лупы, совсем минимализм)

**Рекомендация:** Просто **"Масштаб"** (без иконки, чтобы текст был читаемым на телефоне)

---

## Визуальный пример ожидаемого поведения

**Full режим (текущий, правильный):**
```
┌─────────────────────────┐
│  ○○○○○○○○○○○○○          │  Белый фон
│  ○ 1 2 3 4 5 6 ○        │  Внешние белые кольца
│  ○○○○○○○○●●●●●○         │  
│        ●●7●8●9●10        │  Чёрная зона с белыми
│        ●●●●●●●●          │  кольцами внутри
└─────────────────────────┘
```

**Zoom7 режим (как ДОЛЖНО быть):**
```
┌─────────────────────────┐
│                         │
│    ████████████████     │  Чёрный круг (зона 7)
│    ██○7○○8○○9○○10      │  заполняет экран
│    ██○○○○○○○○○○        │  Белые кольца 8-9-10
│    ████████████████     │  внутри чёрного
│                         │
└─────────────────────────┘
```

**Zoom7 режим (как ЕСТЬ СЕЙЧАС — БАГ):**
```
┌─────────────────────────┐
│                         │
│    ○○○○○○○○○○○○○        │  Белый круг
│    ○○7○○8○○9○○10       │  Нет чёрной зоны!
│    ○○○○○○○○○○○○○        │  Выглядит странно
│                         │
└─────────────────────────┘
```

---

## Критерии приёмки

### Визуал
- ✅ В zoom7 режиме видна **чёрная зона** (сплошной чёрный круг)
- ✅ Внутри чёрной зоны белые **кольца 8, 9, 10**
- ✅ В центре белая **точка** (внутренняя десятка)
- ✅ Выглядит как **увеличенная фотография** центра мишени
- ✅ Full режим не сломался — остался как был

### UI кнопка
- ✅ Текст кнопки: **"Масштаб"** (вместо "1-10"/"7-10")
- ✅ Клик по кнопке переключает zoom циклически (как было)
- ✅ Кнопка одинаково выглядит в обоих режимах (текст не меняется)

### Тесты
- ✅ Все 254 теста проходят
- ✅ TypeScript компиляция чистая

---

## Изменяемые файлы

### src/components/TargetCanvas.tsx
**Одна строка:**
```typescript
// Убрать условие fill:
- <circle r={RING_D[7] / 2 * ZOOM_SCALE} fill={isZoom7 ? 'none' : 'black'} />
+ <circle r={RING_D[7] / 2 * ZOOM_SCALE} fill="black" />
```

### src/screens/TrainingScreen.tsx
**Убрать ZOOM_LABELS, упростить кнопку:**
```typescript
// Убрать:
- const ZOOM_LABELS: Record<string, string> = { full: '1-10', zoom7: '7-10' };

// Изменить кнопку:
- <button>{ZOOM_LABELS[zoomMode]}</button>
+ <button>Масштаб</button>
```

---

## НЕ меняются

- `src/scoring.ts` — не затронут
- `src/transform.ts` — не затронут
- `src/db/*` — не затронут
- Логика масштабирования (ZOOM_SCALE) — остаётся как есть

---

## Приоритет

**HIGH** — функционал в production, но визуально выглядит неправильно (нет чёрной зоны).

---

## Назначение

**Worker** с чистым контекстом.

**Задача простая:**
1. Одна строка в TargetCanvas: убрать `fill={isZoom7 ? 'none' : 'black'}` → сделать `fill="black"`
2. Одна строка в TrainingScreen: заменить `{ZOOM_LABELS[zoomMode]}` → `Масштаб`
3. Удалить неиспользуемую константу `ZOOM_LABELS`
4. Запустить тесты
5. Готово!

---

## Исправление (2024-08-19)

**Worker run:** 48445726 (59854048-0faf-420c-86aa-c144e80edd00)

**Реализовано:**

### 1. Чёрная зона теперь видна (TargetCanvas.tsx)
```typescript
// БЫЛО (БАГ):
<circle r={RING_D[7] / 2 * ZOOM_SCALE} fill={isZoom7 ? 'none' : 'black'} />

// СТАЛО (ИСПРАВЛЕНО):
<circle r={RING_D[7] / 2 * ZOOM_SCALE} fill="black" />
```

**Эффект:**
- Full режим: ZOOM_SCALE=1.0, радиус=29.75mm, чёрный круг внутри белого ✅
- Zoom7 режим: ZOOM_SCALE=2.69, радиус=80mm, чёрный круг заполняет весь viewBox ✅
- Внутри чёрного круга белые кольца 8-9-10 — выглядит как zoom существующей мишени ✅

### 2. Упрощена UI кнопка (TrainingScreen.tsx)
```typescript
// Удалено:
- const ZOOM_LABELS: Record<string, string> = { full: '1-10', zoom7: '7-10' };

// Изменено:
- <button>{ZOOM_LABELS[zoomMode]}</button>
+ <button>Масштаб</button>
```

**Эффект:**
- Одна кнопка "Масштаб" вместо меняющегося текста "1-10"/"7-10" ✅
- Интуитивнее и проще ✅
- Текст не меняется при переключении режимов ✅

### 3. Тесты
- ✅ Все 254 теста проходят
- ✅ TypeScript компиляция чистая
- ✅ Build успешен (188.37 kB)

**Изменённые файлы:**
- `src/components/TargetCanvas.tsx` — убрано условие fill (1 строка)
- `src/screens/TrainingScreen.tsx` — удалён ZOOM_LABELS, кнопка "Масштаб" (2 строки)

**Визуальный результат:**

Full режим:
- Белый фон, внешние кольца 1-6, чёрная зона с кольцами 7-10 внутри ✅

Zoom7 режим:
- Чёрный круг заполняет экран ✅
- Белые кольца 8-9-10 внутри чёрного ✅
- Белая точка в центре ✅
- Выглядит как увеличенная фотография центра мишени ✅

**Deployment:**
- Commit: `d27eed3`
- Production: https://musketoon-coach.pages.dev
- Preview: https://5a2f55f6.musketoon-coach.pages.dev
- Status: ✅ LIVE

**Проверка:**
1. Открой https://musketoon-coach.pages.dev
2. Создай/открой тренировку
3. Кликни "Масштаб" → zoom режим
4. **Видна чёрная зона** с белыми кольцами внутри ✅
5. Выглядит как zoom существующей мишени ✅
